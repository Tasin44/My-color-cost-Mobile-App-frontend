/// RevenueCat Initialization Example for main.dart
///
/// Add this code to your main.dart file to initialize RevenueCat
/// when your app starts

/*
import 'package:color_os/app/core/services/revenue_cat_service.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  
  // Initialize RevenueCat early in app lifecycle
  try {
    await RevenueCatService().initialize();
    print('RevenueCat initialized successfully');
  } catch (e) {
    print('RevenueCat initialization failed: $e');
    // App can still continue without RevenueCat
  }
  
  runApp(const MyApp());
}

// Optional: Login user after authentication
void loginUserToRevenueCat(String userId) async {
  try {
    await RevenueCat Service().login(userId);
    print('User logged in to RevenueCat: $userId');
  } catch (e) {
    print('RevenueCat login failed: $e');
  }
}
*/
